services:
  postgres:
    image: postgres:17

    restart: unless-stopped

    env_file:
      - .env

    volumes:
      - /opt/atlas/data/postgres:/var/lib/postgresql/data

    ports:
      - "5432:5432"

    networks:
      - atlas-network

networks:
  atlas-network:
    external: true